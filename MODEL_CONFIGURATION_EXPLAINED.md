# อธิบายการตั้งค่า Model ใน Luna AI

## ⚠️ สิ่งสำคัญ: Code ไม่ใช้ `PRIMARY_MODEL`!

โค้ดใช้:
- `SIMPLE_MODEL` - สำหรับคำถามง่าย
- `COMPLEX_MODEL` - สำหรับคำถามยาก
- `FALLBACK_MODEL` - ใช้เมื่อ model หลักล้มเหลว

**`PRIMARY_MODEL` ไม่ได้ถูกใช้ในโค้ด!**

## วิธีการทำงาน

1. **คำถามง่าย** → ใช้ `SIMPLE_MODEL` (default: `gpt-4o-mini`)
2. **คำถามยาก** → ใช้ `COMPLEX_MODEL` (default: `anthropic/claude-3-opus`)
3. **ถ้า model หลักล้มเหลว** → ใช้ `FALLBACK_MODEL` (default: `gpt-4o-mini`)

## การตั้งค่าสำหรับ OpenRouter

ถ้าใช้ `OPENROUTER_KEY` ต้องใช้ model name ที่มี prefix:

### ✅ ถูกต้อง:
```
SIMPLE_MODEL=openai/gpt-4o-mini
COMPLEX_MODEL=anthropic/claude-3-opus
FALLBACK_MODEL=openai/gpt-4o-mini
```

### ❌ ผิด:
```
SIMPLE_MODEL=gpt-4o-mini  # ไม่มี prefix
COMPLEX_MODEL=gpt-4o      # ไม่มี prefix
```

## Model ที่แนะนำสำหรับ OpenRouter

### สำหรับคำถามง่าย (SIMPLE_MODEL):
- `openai/gpt-4o-mini` - เร็วและประหยัด
- `openai/gpt-3.5-turbo` - ถูกที่สุด

### สำหรับคำถามยาก (COMPLEX_MODEL):
- `anthropic/claude-3-opus` - แข็งแกร่งที่สุด (แนะนำ!)
- `anthropic/claude-3-sonnet` - ดีและเร็วกว่า Opus
- `openai/gpt-4o` - เร็วและดี

### สำหรับ Fallback (FALLBACK_MODEL):
- `openai/gpt-4o-mini` - เร็วและประหยัด
- `openai/gpt-4o` - ดีกว่าแต่แพงกว่า

## ตัวอย่างการตั้งค่าใน .env

```env
# ใช้ OpenRouter
OPENROUTER_KEY=sk-or-v1-xxxxxxxxxxxxx

# Model สำหรับคำถามง่าย
SIMPLE_MODEL=openai/gpt-4o-mini

# Model สำหรับคำถามยาก (แนะนำ!)
COMPLEX_MODEL=anthropic/claude-3-opus

# Model สำรอง (ใช้เมื่อ model หลักล้มเหลว)
FALLBACK_MODEL=openai/gpt-4o-mini
```

## ตรวจสอบว่าใช้ model อะไร

ดู log เมื่อส่งข้อความ:
```
[ai] 💬 Simple question → using openai/gpt-4o-mini
[ai] 🔍 Complex question detected → using anthropic/claude-3-opus
```

## หมายเหตุ

- `PRIMARY_MODEL` ไม่ได้ถูกใช้ - ลบออกได้
- ต้องมี prefix `openai/` หรือ `anthropic/` เมื่อใช้ OpenRouter
- `FALLBACK_MODEL` ควรเป็น model ที่ถูกกว่าและเร็วกว่า (ใช้เมื่อ model หลักล้มเหลว)



