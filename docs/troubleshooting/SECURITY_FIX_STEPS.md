# 🔒 แก้ไขปัญหาความปลอดภัย - API Key ถูกเปิดเผย

## ⚠️ สถานการณ์
OpenRouter แจ้งเตือนว่า API key ถูกเปิดเผยใน GitHub repository ที่เป็น public

**URL ที่พบ:** `https://github.com/NOI56/Luna-moon/blob/5dcc3da73ea2fce4aea5cdf522aa1cf48863b0ec/.env`

---

## ✅ วิธีแก้ไข (ทำตามลำดับ)

### ขั้นตอนที่ 1: Force Push ไป GitHub (ลบไฟล์ออกจาก History)

```powershell
# Force push เพื่อลบไฟล์ .env ออกจาก GitHub
git push origin --force --all
```

**⚠️ หมายเหตุ:** 
- การ force push จะเขียนทับ history บน GitHub
- ไฟล์ `.env` จะถูกลบออกจาก Git history
- API key เก่าจะยังอยู่ใน history เก่า (ถ้ามีคน clone ไปแล้ว)

---

### ขั้นตอนที่ 2: ตรวจสอบว่า `.env` อยู่ใน `.gitignore`

ไฟล์ `.env` อยู่ใน `.gitignore` แล้ว ✅

**ตรวจสอบ:**
```powershell
git check-ignore .env
```

**ถ้าแสดง path ของ `.env`** = ถูกต้อง (Git จะ ignore ไฟล์นี้)

---

### ขั้นตอนที่ 3: สร้าง API Key ใหม่

1. **ไปที่:** [openrouter.ai/keys](https://openrouter.ai/keys)
2. **ลบ key เก่า** (ถ้ายังมี)
3. **สร้าง key ใหม่**
4. **คัดลอก key ใหม่**

---

### ขั้นตอนที่ 4: อัปเดต `.env` ในเครื่อง

1. **เปิดไฟล์ `.env`** ในเครื่อง
2. **อัปเดต API key ใหม่:**
   ```
   OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxx
   ```
3. **บันทึกไฟล์**
4. **อย่า commit ไฟล์นี้!** (มันอยู่ใน `.gitignore` แล้ว)

---

### ขั้นตอนที่ 5: ตั้งค่า Environment Variables ใน Railway

**เมื่อ deploy บน Railway:**

1. **ไปที่ Railway Dashboard**
2. **เลือก Project → Variables**
3. **เพิ่ม Environment Variable:**
   - **Key:** `OPENROUTER_API_KEY`
   - **Value:** (ใส่ API key ใหม่)
4. **คลิก "Add"**

**วิธีนี้:**
- ✅ API key ไม่ถูก commit ไป GitHub
- ✅ ปลอดภัยกว่า
- ✅ ใช้ได้กับทุก environment

---

## 📋 Checklist

- [ ] Force push ไป GitHub (ลบ `.env` ออกจาก history)
- [ ] ตรวจสอบว่า `.env` อยู่ใน `.gitignore` ✅
- [ ] สร้าง API key ใหม่ที่ OpenRouter
- [ ] อัปเดต `.env` ในเครื่อง (ไม่ commit)
- [ ] ตั้งค่า Environment Variables ใน Railway (เมื่อ deploy)

---

## 🛡️ Best Practices สำหรับอนาคต

### 1. ใช้ `.env.example` สำหรับ Template
```bash
# .env.example (commit ได้)
OPENROUTER_API_KEY=your_api_key_here
```

### 2. ตรวจสอบก่อน Commit
```powershell
# ตรวจสอบว่าไม่มี sensitive data
git diff --cached | Select-String -Pattern "api|key|secret|password"
```

### 3. ใช้ Environment Variables ใน Cloud
- **Railway:** Variables tab
- **Render:** Environment tab
- **ไม่ต้อง commit `.env`**

### 4. ใช้ Pre-commit Hooks
```bash
# .git/hooks/pre-commit
#!/bin/sh
if git diff --cached --name-only | grep -q "\.env$"; then
  echo "❌ Error: .env file cannot be committed!"
  exit 1
fi
```

---

## ⚠️ หมายเหตุสำคัญ

**หลังจาก Force Push:**
- History บน GitHub จะถูกเขียนทับ
- ไฟล์ `.env` จะถูกลบออกจาก history
- **แต่:** ถ้ามีคน clone repository ไปแล้ว พวกเขายังมี history เก่าอยู่
- **แนะนำ:** แจ้งให้ทุกคนที่ clone repository ไปแล้วว่าให้ pull ใหม่

**API Key เก่า:**
- OpenRouter ได้ปิดใช้งานแล้ว ✅
- **ต้องสร้างใหม่** และอัปเดตใน `.env` และ Railway

---

## 🚀 หลังจากแก้ไขเสร็จ

1. **Deploy บน Railway** (ตามขั้นตอนเดิม)
2. **ตั้งค่า Environment Variables** ใน Railway dashboard
3. **ทดสอบว่าเว็บทำงานได้**

---

**ทำตามขั้นตอนด้านบนเพื่อแก้ไขปัญหาความปลอดภัย!** 🔒

