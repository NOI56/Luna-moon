# 🚀 Railway Deploy - Quick Start Guide

## 📋 สิ่งที่ต้องทำ (3 ขั้นตอน)

### ✅ ขั้นตอนที่ 1: สร้าง GitHub Repository

1. **ไปที่ GitHub:**
   - เปิด [github.com](https://github.com)
   - Login (หรือสร้างบัญชีใหม่)

2. **สร้าง Repository ใหม่:**
   - คลิก "+" (มุมขวาบน) → "New repository"
   - ตั้งชื่อ: `luna-rps-game` (หรือชื่ออื่น)
   - เลือก "Public" หรือ "Private"
   - **อย่า** check "Add a README file"
   - คลิก "Create repository"

3. **คัดลอก Repository URL:**
   - GitHub จะแสดง URL เช่น: `https://github.com/YOUR_USERNAME/luna-rps-game.git`
   - **คัดลอก URL นี้ไว้** (จะใช้ในขั้นตอนต่อไป)

---

### ✅ ขั้นตอนที่ 2: Push Code ไป GitHub

**เปิด PowerShell ในโฟลเดอร์โปรเจกต์:**

```powershell
# ไปที่โฟลเดอร์โปรเจกต์
cd D:\LunaAI_v10_Project

# 1. เริ่ม Git repository
git init

# 2. เพิ่มไฟล์ทั้งหมด (ยกเว้น node_modules)
git add .

# 3. Commit
git commit -m "Initial commit - Ready for Railway"

# 4. เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME และ REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/luna-rps-game.git

# 5. Push ไป GitHub
git branch -M main
git push -u origin main
```

**หมายเหตุ:** 
- แทน `YOUR_USERNAME` ด้วย username GitHub ของคุณ
- แทน `luna-rps-game` ด้วยชื่อ repository ที่สร้าง

**ถ้า GitHub ต้องการ username/password:**
- ใช้ **Personal Access Token** แทน password
- สร้างได้ที่: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- เลือก scope: `repo` (full control)

---

### ✅ ขั้นตอนที่ 3: Deploy บน Railway

1. **กลับไปที่ Railway Dashboard:**
   - เปิด [railway.app/dashboard](https://railway.app/dashboard)

2. **คลิก "Deploy from GitHub repo":**
   - คลิกไอคอน **GitHub** (ตัวแรกในแถว 4 ตัว)
   - หรือคลิก "Deploy a GitHub Repository"

3. **Login GitHub (ถ้ายังไม่ได้ login):**
   - Railway จะเปิดหน้าต่างใหม่
   - คลิก "Authorize Railway"
   - อนุญาตให้ Railway เข้าถึง repositories

4. **เลือก Repository:**
   - Railway จะแสดงรายการ repositories
   - **เลือก `luna-rps-game`** (หรือชื่อที่คุณสร้าง)

5. **Railway จะ Deploy อัตโนมัติ:**
   - รอประมาณ 2-3 นาที
   - ดู progress ใน dashboard
   - Railway จะ detect `railway.json` และ deploy ให้

6. **ดู URL:**
   - ไปที่ tab "Settings"
   - ดู "Domains" section
   - Railway จะให้ URL เช่น: `https://luna-rps-game-production.up.railway.app`
   - **คัดลอก URL นี้ไว้**

---

## 🎉 เสร็จแล้ว!

### ทดสอบเว็บ:
- **Betting Mode:** `https://your-app.railway.app/rps_betting.html`
- **PvP Mode:** `https://your-app.railway.app/rps_game.html`
- **VS Luna Mode:** `https://your-app.railway.app/rps_vs_luna.html`

### แชร์ URL ให้คนอื่น:
- แชร์ URL ที่ได้จาก Railway
- คนอื่นสามารถเข้าถึงได้จากทุกที่!

---

## 🔄 วิธีแก้ไขโค้ดในอนาคต

1. **แก้ไขโค้ดในเครื่อง**
2. **Push ไป GitHub:**
   ```powershell
   git add .
   git commit -m "แก้ไข UI หรือเพิ่มฟีเจอร์"
   git push origin main
   ```
3. **Railway จะ Deploy อัตโนมัติ** (รอ 2-3 นาที)
4. **เว็บอัปเดตทันที!**

---

## ⚠️ ถ้ามีปัญหา

### ปัญหา 1: Git push ไม่ได้
**แก้ไข:**
- ตรวจสอบว่า repository URL ถูกต้อง
- ใช้ Personal Access Token แทน password
- ตรวจสอบว่า repository เป็น Public หรือมีสิทธิ์เข้าถึง

### ปัญหา 2: Railway ไม่เจอ repository
**แก้ไข:**
- ตรวจสอบว่า authorize Railway แล้ว
- Refresh หน้า Railway
- ลอง logout และ login ใหม่

### ปัญหา 3: Deploy ไม่สำเร็จ
**แก้ไข:**
- ดู logs ใน tab "Deployments"
- ตรวจสอบว่า `railway.json` มีอยู่
- ตรวจสอบว่า `package.json` มี `start` script

---

## 📝 Checklist

- [ ] สร้าง GitHub repository แล้ว
- [ ] Push code ไป GitHub แล้ว
- [ ] คลิก "Deploy from GitHub repo" ใน Railway
- [ ] Login GitHub และ authorize Railway
- [ ] เลือก repository ที่ถูกต้อง
- [ ] Railway deploy สำเร็จ
- [ ] ได้ URL จาก Railway
- [ ] ทดสอบเว็บว่าทำงานได้

---

**พร้อมแล้ว! เริ่มทำตามขั้นตอนได้เลย!** 🚀

