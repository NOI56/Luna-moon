# 🔧 ตั้งค่า Environment Variables ใน Railway

## 📍 วิธีหา Environment Variables ใน Railway

### ขั้นตอนที่ 1: เข้า Railway Dashboard
1. ไปที่ [railway.app](https://railway.app)
2. Login ด้วย GitHub
3. **เลือก Project** ที่คุณ deploy (เช่น `Luna-moon`)

### ขั้นตอนที่ 2: หา Variables Tab
1. **คลิกที่ Service** (ชื่อ service ที่ deploy)
2. **ไปที่ tab "Variables"** (อยู่ด้านบน ใกล้กับ "Deployments", "Settings")
3. หรือ **คลิกที่ Settings** → **ดู "Variables" section**

**ถ้าหาไม่เจอ:**
- ดูที่ **sidebar ด้านซ้าย** → **Variables**
- หรือ **Settings** → **Environment Variables**

---

## ✅ Environment Variables ที่ต้องตั้งค่า

### 1. OPENROUTER_KEY (สำคัญที่สุด!)

**วิธีตั้งค่า:**
1. ไปที่ **Variables tab**
2. คลิก **"+ New Variable"** หรือ **"Add Variable"**
3. ใส่:
   - **Key:** `OPENROUTER_KEY`
   - **Value:** `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`
4. คลิก **"Add"** หรือ **"Save"**

**⚠️ หมายเหตุ:** 
- ใช้ `OPENROUTER_KEY` (ไม่ใช่ `OPENROUTER_API_KEY`)
- Code ใช้ `process.env.OPENROUTER_KEY`

---

### 2. Environment Variables อื่นๆ (ถ้าต้องการ)

#### สำหรับ VTube Studio (ถ้าใช้):
- **Key:** `VTS_ENABLED`
- **Value:** `true` หรือ `false`

- **Key:** `VTS_AUTH_TOKEN`
- **Value:** (token จาก `node scripts/vts/vts-auth.cjs`)

#### สำหรับ TTS (ถ้าใช้):
- **Key:** `TTS_ENABLED`
- **Value:** `true` หรือ `false`

- **Key:** `ELEVEN_KEY`
- **Value:** (ElevenLabs API key)

#### สำหรับ Database:
- **Key:** `DB_DRIVER`
- **Value:** `sqlite` (default)

- **Key:** `DB_SQLITE_PATH`
- **Value:** `./tmp/luna.db` (default)

#### อื่นๆ:
- **Key:** `PORT`
- **Value:** (Railway จะกำหนดให้อัตโนมัติ - ไม่ต้องตั้ง)

- **Key:** `NODE_ENV`
- **Value:** `production` (แนะนำ)

---

## 🎯 ตัวอย่างการตั้งค่าใน Railway

### วิธีที่ 1: ตั้งค่าผ่าน Variables Tab

1. **ไปที่ Project → Service → Variables**
2. **คลิก "+ New Variable"**
3. **เพิ่มแต่ละ variable:**

```
OPENROUTER_KEY = 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
NODE_ENV = production
```

4. **คลิก "Save" หรือ "Deploy"**

---

### วิธีที่ 2: ตั้งค่าผ่าน Settings

1. **ไปที่ Project → Service → Settings**
2. **ดู "Environment Variables" section**
3. **คลิก "Add Variable"**
4. **เพิ่ม variables ตามด้านบน**

---

## 📋 Checklist

- [ ] หา Variables tab ใน Railway dashboard
- [ ] เพิ่ม `OPENROUTER_KEY` = `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`
- [ ] (ถ้าต้องการ) เพิ่ม `NODE_ENV` = `production`
- [ ] (ถ้าต้องการ) เพิ่ม variables อื่นๆ
- [ ] Deploy หรือ Restart service
- [ ] ทดสอบว่าเว็บทำงาน

---

## 🔍 วิธีตรวจสอบว่า Variables ตั้งค่าถูกต้อง

### 1. ดูใน Railway Dashboard
- ไปที่ **Variables tab**
- ตรวจสอบว่า `OPENROUTER_KEY` มีอยู่และมีค่า

### 2. ดู Logs
- ไปที่ **Deployments tab**
- ดู **Logs**
- ตรวจสอบว่าไม่มี error เกี่ยวกับ API key

### 3. ทดสอบเว็บ
- เปิด URL ที่ Railway ให้
- ทดสอบว่า API ทำงาน

---

## ⚠️ ข้อผิดพลาดที่พบบ่อย

### 1. หา Variables tab ไม่เจอ
**แก้ไข:**
- ดูที่ **sidebar ด้านซ้าย** → **Variables**
- หรือ **Settings** → **Environment Variables**
- หรือ **Service** → **Variables tab**

### 2. ตั้งชื่อ Variable ผิด
**แก้ไข:**
- ใช้ `OPENROUTER_KEY` (ไม่ใช่ `OPENROUTER_API_KEY`)
- ตรวจสอบว่าไม่มี space หรือ typo

### 3. Value ไม่ถูกต้อง
**แก้ไข:**
- ตรวจสอบว่า API key ถูกต้อง
- ไม่มี space หรือ newline
- คัดลอก key ทั้งหมด

### 4. Variables ไม่ทำงาน
**แก้ไข:**
- **Restart service** (คลิก "Redeploy" หรือ "Restart")
- ตรวจสอบ logs ว่า variables ถูกโหลดหรือไม่

---

## 🚀 หลังจากตั้งค่าเสร็จ

1. **Restart Service:**
   - ไปที่ **Deployments tab**
   - คลิก **"Redeploy"** หรือ **"Restart"**

2. **ตรวจสอบ Logs:**
   - ดู logs ว่าไม่มี error
   - ตรวจสอบว่า API key ถูกโหลด

3. **ทดสอบเว็บ:**
   - เปิด URL ที่ Railway ให้
   - ทดสอบว่า API ทำงาน

---

## 💡 Tips

1. **ใช้ Variables แทน `.env`:**
   - ปลอดภัยกว่า
   - ไม่ต้อง commit ไป GitHub
   - เปลี่ยนได้ง่าย

2. **ตั้งค่า Variables ก่อน Deploy:**
   - ตั้งค่า variables ก่อน deploy
   - Service จะใช้ variables ที่ตั้งค่าไว้

3. **Backup Variables:**
   - จดบันทึก variables ที่ตั้งค่าไว้
   - เก็บไว้ในที่ปลอดภัย

---

**ตั้งค่า Environment Variables ใน Railway แล้ว Deploy ได้เลย!** 🚀

