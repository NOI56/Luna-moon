# 🎭 Luna Character Viewer - คำแนะนำการใช้งาน

## ✅ สิ่งที่ต้องทำ

### 1. รัน Server (จำเป็น!)

```bash
node index.js
```

**ตรวจสอบว่า server ทำงาน:**
- เปิด Browser ไปที่: `http://localhost:8787`
- ควรเห็นหน้าเว็บ Luna AI

---

## ❌ สิ่งที่**ไม่ต้อง**ทำ

### **ไม่ต้องเปิด VTube Studio!**

Character Viewer (`/luna-character`) เป็น **standalone web viewer** ที่:
- ✅ ทำงานได้โดยไม่ต้องใช้ VTube Studio
- ✅ ใช้ Live2D Web SDK โดยตรง
- ✅ รับข้อมูลจาก Luna AI backend ผ่าน WebSocket

**หมายเหตุ:** 
- VTube Studio ใช้สำหรับ **VTS features** (emotions, hotkeys, mouth movement) เท่านั้น
- Character Viewer ใช้สำหรับ **แสดงตัวละครใน OBS** โดยไม่ต้องใช้ VTS

---

## 🎨 Character Viewer

### เปิด Character Viewer:

```
http://localhost:8787/luna-character?model=Akari
```

### ใช้ใน OBS:

1. **เพิ่ม Browser Source** ใน OBS
2. **URL:** `http://localhost:8787/luna-character?model=Akari`
3. **Width:** `1920`
4. **Height:** `1080`
5. **✅ Shutdown source when not visible** (ถ้าต้องการ)
6. **✅ Refresh browser when scene becomes active** (ถ้าต้องการ)

---

## 🐛 ปัญหาที่พบ

### ปัญหา: `index.min.js` (pixi-live2d-display) โหลดไม่สำเร็จ

**สาเหตุ:** CDN ไม่สามารถเข้าถึงได้ (อาจเป็นปัญหา network/firewall)

**วิธีแก้ชั่วคราว:**
- Character Viewer จะใช้ **placeholder character** (ตัวละครวาดด้วย Canvas) แทน
- Placeholder character ยังทำงานได้:
  - ✅ กระพริบตา
  - ✅ หายใจ
  - ✅ เคลื่อนไหวตาม emotion
  - ✅ รับข้อมูลจาก Luna AI backend

**วิธีแก้ถาวร (ถ้าต้องการใช้ Live2D):**
1. ดาวน์โหลด `pixi-live2d-display` library มาเก็บในโปรเจค
2. แก้ไข `luna_character.html` ให้ใช้ local file แทน CDN

---

## 📋 สรุป

| สิ่งที่ต้องทำ | สถานะ |
|------------|-------|
| รัน `node index.js` | ✅ จำเป็น |
| เปิด VTube Studio | ❌ ไม่จำเป็น (สำหรับ Character Viewer) |
| เปิด Browser | ✅ จำเป็น (เพื่อดู Character Viewer) |
| ใช้ใน OBS | ✅ ทำงานได้ (Browser Source) |

---

## 💡 คำแนะนำ

**ตอนนี้ Character Viewer ใช้ placeholder character** ซึ่ง:
- ✅ ทำงานได้ทันที
- ✅ ไม่ต้องรอ CDN
- ✅ มีการเคลื่อนไหวเหมือนคน

**ถ้าต้องการใช้ Live2D จริงๆ:**
- ต้องแก้ปัญหา CDN ก่อน
- หรือดาวน์โหลด library มาเก็บในโปรเจค

---

## 🔧 Debug Mode

เพิ่ม `?debug=true` เพื่อดู debug info:

```
http://localhost:8787/luna-character?model=Akari&debug=true
```

จะแสดง:
- Status
- Emotion
- Mouth (0-1)
- Eye (0-1)
- Energy (0-1)






