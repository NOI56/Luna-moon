# 🪙 วิธีสร้างเหรียญ Luna บน Pump.fun และตั้งค่าใน .env

## 🎯 สิ่งที่ต้องทำ

1. **สร้าง token บน Pump.fun**
2. **คัดลอก Token Mint Address**
3. **ใส่ใน `.env`**

---

## 🚀 ขั้นตอนที่ 1: สร้าง Token บน Pump.fun

### 1. ไปที่ Pump.fun

1. **เปิด:** [pump.fun](https://pump.fun)
2. **เชื่อมต่อ Wallet:**
   - คลิก "Connect Wallet"
   - เลือก Phantom, Solflare, หรือ wallet อื่น
   - อนุมัติการเชื่อมต่อ

---

### 2. สร้าง Token

1. **คลิก "Create"** หรือ **"Create Token"**
2. **กรอกข้อมูล:**
   - **Name:** `Luna` (หรือชื่ออื่น)
   - **Symbol:** `LUNA` (หรือ symbol อื่น)
   - **Description:** คำอธิบายเกี่ยวกับ token
   - **Image:** อัปโหลดรูป (ถ้าต้องการ)
   - **Website:** URL เว็บไซต์ (ถ้ามี)
   - **Twitter:** Twitter handle (ถ้ามี)
   - **Telegram:** Telegram link (ถ้ามี)

3. **คลิก "Create Token"**

---

### 3. จ่ายค่าธรรมเนียม

- Pump.fun จะแสดงค่าธรรมเนียม (ประมาณ 0.02-0.05 SOL)
- **อนุมัติ transaction** ใน wallet
- **รอให้ transaction สำเร็จ**

---

## 📋 ขั้นตอนที่ 2: คัดลอก Token Mint Address

### หลังจากสร้าง Token สำเร็จ:

1. **ดู Token Page:**
   - Pump.fun จะแสดงหน้า token ที่สร้าง
   - หรือไปที่ "My Tokens" → เลือก token ที่สร้าง

2. **หา Token Mint Address:**
   - ดูที่ **"Token Address"** หรือ **"Mint Address"**
   - หรือดูที่ URL: `https://pump.fun/TOKEN_MINT_ADDRESS`
   - **คัดลอก mint address** (ตัวอย่าง: `CbB4ivri6wLfqx4NwrWY3ArD7mXv1e91HeYeq3KBpump`)

---

## ⚙️ ขั้นตอนที่ 3: ใส่ใน `.env`

### 1. เปิดไฟล์ `.env`

```bash
# เปิด .env ใน editor
```

### 2. เพิ่ม Token Mint Address

```env
# Luna Token Mint Address จาก Pump.fun
LUNA_TOKEN_MINT=CbB4ivri6wLfqx4NwrWY3ArD7mXv1e91HeYeq3KBpump
```

**⚠️ หมายเหตุ:**
- แทน `CbB4ivri6wLfqx4NwrWY3ArD7mXv1e91HeYeq3KBpump` ด้วย mint address จริงของคุณ
- อย่า commit `.env` ไป GitHub

---

## ✅ ตรวจสอบว่าใช้งานได้

### 1. ตรวจสอบในโค้ด

```javascript
// ตรวจสอบว่า LUNA_TOKEN_MINT ถูกตั้งค่า
const LUNA_TOKEN_MINT = process.env.LUNA_TOKEN_MINT;
if (!LUNA_TOKEN_MINT) {
  console.error("LUNA_TOKEN_MINT not found in .env");
}
```

### 2. ทดสอบใน RPS Game

- เปิด RPS Game
- เชื่อมต่อ Wallet
- ตรวจสอบว่า balance แสดงถูกต้อง

---

## 📋 Checklist

- [ ] สร้าง token บน Pump.fun
- [ ] คัดลอก Token Mint Address
- [ ] ใส่ `LUNA_TOKEN_MINT` ใน `.env`
- [ ] ตรวจสอบว่าใช้งานได้

---

## 💡 Tips

1. **เก็บ Mint Address ไว้:**
   - บันทึก mint address ไว้ในที่ปลอดภัย
   - ใช้ mint address นี้ในทุกที่ที่ต้องการใช้ token

2. **ตรวจสอบ Mint Address:**
   - ตรวจสอบว่า mint address ถูกต้อง
   - ใช้ Solana Explorer: https://solscan.io/token/MINT_ADDRESS

3. **แชร์ Mint Address:**
   - แชร์ mint address ให้ผู้ใช้
   - ผู้ใช้สามารถเพิ่ม token ใน wallet ได้

---

## 🎯 สรุป

**วิธีง่ายๆ:**
1. สร้าง token บน Pump.fun
2. คัดลอก mint address
3. ใส่ใน `.env` → `LUNA_TOKEN_MINT=mint_address`

**เสร็จแล้ว!** 🚀

