# 🖼️ วิธีใช้ภาพ Luna เป็น Placeholder Character

## 📋 ขั้นตอน

### 1. เตรียมไฟล์ภาพ Luna

1. **เตรียมไฟล์ภาพ Luna** (PNG หรือ JPG)
   - ควรเป็นภาพที่มีพื้นหลังโปร่งใส (transparent) หรือ green screen
   - ขนาดแนะนำ: 1920x1080 หรือใหญ่กว่า
   - ชื่อไฟล์: `luna.png` หรือ `luna.jpg`

2. **วางไฟล์ในโฟลเดอร์:**
   ```
   D:\LunaAI_v10_Project\public\images\luna.png
   ```
   หรือ
   ```
   D:\LunaAI_v10_Project\public\images\luna.jpg
   ```

### 2. ตรวจสอบว่าไฟล์อยู่ถูกที่

เปิด File Explorer ไปที่:
```
D:\LunaAI_v10_Project\public\images\
```

ควรมีไฟล์ `luna.png` หรือ `luna.jpg` อยู่

### 3. Restart Server

หลังจากวางไฟล์แล้ว:
1. หยุด server (กด `Ctrl + C` ใน terminal)
2. รันใหม่: `node index.js`

### 4. รีเฟรช Character Viewer

เปิด Browser ไปที่:
```
http://localhost:8787/luna-character?model=Akari
```

กด `Ctrl + Shift + R` (hard refresh)

---

## ✅ หลังจากทำเสร็จ

Character Viewer จะแสดงภาพ Luna แทน placeholder character ที่วาดด้วย Canvas

---

## 💡 หมายเหตุ

- ถ้า Live2D model โหลดสำเร็จ → จะใช้ Live2D แทนภาพ
- ถ้า Live2D ไม่โหลด → จะใช้ภาพ Luna เป็น placeholder
- ภาพจะมีการเคลื่อนไหวตาม emotion, mouth, eye จาก WebSocket

---

## 🐛 ถ้ายังไม่เห็นภาพ

1. ตรวจสอบว่าไฟล์อยู่ถูกที่: `public/images/luna.png`
2. ตรวจสอบชื่อไฟล์ (ต้องเป็น `luna.png` หรือ `luna.jpg`)
3. เปิด Browser Console (F12) ดู error messages
4. ตรวจสอบ Network tab ว่าไฟล์ภาพโหลดสำเร็จหรือไม่






