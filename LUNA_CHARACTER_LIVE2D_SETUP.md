# Luna Character - Live2D Model Setup Guide

## 📖 วิธีหาไฟล์โมเดลจาก VTube Studio

### Windows
1. เปิด File Explorer
2. ไปที่: `%AppData%\VTubeStudio\Live2DModels\`
   - หรือ: `C:\Users\[ชื่อผู้ใช้ของคุณ]\AppData\Roaming\VTubeStudio\Live2DModels\`
3. หาโฟลเดอร์ของโมเดลที่คุณใช้ (เช่น `Luna`, `character_name`, ฯลฯ)
4. ในโฟลเดอร์นั้นจะมีไฟล์:
   - `*.model3.json` (ไฟล์หลักของโมเดล)
   - `*.png` หรือ `*.moc3` (texture files)
   - โฟลเดอร์ `textures/` (ถ้ามี)

### ตัวอย่างโครงสร้าง
```
Live2DModels/
  └── Luna/
      ├── Luna.model3.json    ← ไฟล์นี้สำคัญ!
      ├── textures/
      │   ├── texture_00.png
      │   └── texture_01.png
      └── physics.json (ถ้ามี)
```

## 🚀 วิธีใช้งาน

### 1. คัดลอกไฟล์โมเดล

คัดลอกโฟลเดอร์โมเดลทั้งหมดไปที่:
```
D:\LunaAI_v10_Project\public\models\[ชื่อโมเดล]\
```

ตัวอย่าง:
```
D:\LunaAI_v10_Project\public\models\Luna\
  ├── Luna.model3.json
  ├── textures/
  │   └── texture_00.png
  └── physics.json
```

### 2. ตั้งค่าใน Character Viewer

เปิด `public/luna_character.html` และแก้ไข:
```javascript
// เปลี่ยน path นี้เป็น path ของโมเดลคุณ
const MODEL_PATH = '/models/Luna/Luna.model3.json';
```

### 3. เปิด Character Viewer

```
http://localhost:8787/luna-character?model=Luna
```

หรือระบุโมเดลใน URL:
```
http://localhost:8787/luna-character?model=ชื่อโมเดล
```

## 📝 หมายเหตุ

- ต้องมีไฟล์ `.model3.json` (สำคัญที่สุด!)
- ต้องมีไฟล์ texture (`.png` หรือ `.moc3`)
- ไฟล์ทั้งหมดต้องอยู่ในโฟลเดอร์เดียวกัน
- ต้อง serve ไฟล์ผ่าน web server (Express จะ serve อัตโนมัติจาก `public/`)

## 🔧 Troubleshooting

### โมเดลไม่แสดง
- ตรวจสอบว่าไฟล์ `.model3.json` อยู่ที่ถูกต้อง
- ตรวจสอบ console ใน browser (F12) ดู error
- ตรวจสอบว่า path ในโค้ดถูกต้อง

### Texture ไม่แสดง
- ตรวจสอบว่าไฟล์ texture อยู่ในโฟลเดอร์ `textures/`
- ตรวจสอบว่า path ใน `.model3.json` ถูกต้อง (อาจต้องแก้ไข path ในไฟล์)

### CORS Error
- ตรวจสอบว่า serve ไฟล์ผ่าน Express server
- อย่าเปิดไฟล์ HTML โดยตรง (file://) ต้องใช้ http://localhost:8787






