# 📋 วิธีคัดลอกโมเดลจาก VTube Studio

> 💡 **สำหรับคนไม่มีความรู้:** อ่านไฟล์ `คัดลอกโมเดล-วิธีง่าย.md` แทน (มีคำอธิบายละเอียดกว่า)

## 🎯 วิธีที่ 1: ใช้ Script (แนะนำ)

### ขั้นตอน:
1. เปิด Terminal/PowerShell ในโฟลเดอร์โปรเจค
2. รันคำสั่ง:
   ```bash
   node copy-vts-model.js
   ```
3. Script จะหาโมเดลทั้งหมดใน VTube Studio
4. เลือกโมเดลที่ต้องการ:
   ```bash
   node copy-vts-model.js [ชื่อโมเดล]
   ```
   ตัวอย่าง:
   ```bash
   node copy-vts-model.js Luna
   ```

---

## 🎯 วิธีที่ 2: คัดลอกด้วยมือ (ถ้า Script ไม่เจอ)

### ขั้นตอน:

#### 1. หาโฟลเดอร์โมเดลใน VTube Studio

**Windows:**
1. กด `Win + R`
2. พิมพ์: `%AppData%\VTubeStudio\Live2DModels`
3. กด Enter

หรือไปที่:
```
C:\Users\[ชื่อผู้ใช้ของคุณ]\AppData\Roaming\VTubeStudio\Live2DModels
```

**macOS:**
```
~/Library/Application Support/VTubeStudio/Live2DModels
```

**Linux:**
```
~/.config/VTubeStudio/Live2DModels
```

#### 2. หาโมเดลที่ต้องการ

ในโฟลเดอร์ `Live2DModels` จะมีโฟลเดอร์หลายโฟลเดอร์ (แต่ละโฟลเดอร์ = 1 โมเดล)

ตัวอย่าง:
```
Live2DModels/
  ├── Luna/              ← โมเดล Luna
  │   ├── Luna.model3.json
  │   ├── textures/
  │   └── physics.json
  ├── Character1/        ← โมเดลอื่น
  └── Character2/
```

#### 3. คัดลอกโฟลเดอร์โมเดล

**วิธีที่ 1: ใช้ File Explorer**
1. เปิดโฟลเดอร์โมเดลใน VTube Studio
2. คลิกขวาที่โฟลเดอร์โมเดล (เช่น `Luna`)
3. เลือก "Copy" หรือกด `Ctrl + C`
4. ไปที่: `D:\LunaAI_v10_Project\public\models\`
5. คลิกขวา → "Paste" หรือกด `Ctrl + V`

**วิธีที่ 2: ใช้ Command Line**
```powershell
# Windows PowerShell
Copy-Item "C:\Users\[ชื่อผู้ใช้]\AppData\Roaming\VTubeStudio\Live2DModels\Luna" -Destination "D:\LunaAI_v10_Project\public\models\Luna" -Recurse
```

#### 4. ตรวจสอบไฟล์

หลังจากคัดลอกแล้ว ควรมีโครงสร้างแบบนี้:
```
D:\LunaAI_v10_Project\public\models\Luna\
  ├── Luna.model3.json    ← ไฟล์นี้สำคัญ!
  ├── textures/
  │   └── texture_00.png
  └── physics.json (ถ้ามี)
```

---

## ✅ ตรวจสอบว่าโมเดลพร้อมใช้งาน

### 1. ตรวจสอบไฟล์
- ✅ มีไฟล์ `.model3.json`
- ✅ มีโฟลเดอร์ `textures/` (หรือไฟล์ texture)
- ✅ ไฟล์ทั้งหมดอยู่ในโฟลเดอร์เดียวกัน

### 2. ทดสอบ
เปิดใน Browser:
```
http://localhost:8787/luna-character?model=Luna
```

หรือใช้ใน OBS:
- URL: `http://localhost:8787/luna-character?model=Luna`
- Width: `1920`
- Height: `1080`

---

## 🐛 Troubleshooting

### โมเดลไม่แสดง
1. ตรวจสอบว่าไฟล์ `.model3.json` อยู่ที่ถูกต้อง
2. ตรวจสอบ console ใน browser (F12) ดู error
3. ตรวจสอบว่า path ใน URL ถูกต้อง (`?model=ชื่อโมเดล`)

### Script ไม่เจอโมเดล
- ตรวจสอบว่า VTube Studio ติดตั้งแล้ว
- ตรวจสอบว่า path ถูกต้อง
- ใช้วิธีที่ 2 (คัดลอกด้วยมือ) แทน

### CORS Error
- ตรวจสอบว่า serve ไฟล์ผ่าน Express server
- อย่าเปิดไฟล์ HTML โดยตรง (file://)
- ต้องใช้ `http://localhost:8787`

---

## 📝 หมายเหตุ

- ชื่อโฟลเดอร์โมเดลจะถูกใช้เป็น `model` parameter ใน URL
- ถ้ามีหลายโมเดล สามารถคัดลอกหลายตัวได้
- โมเดลจะถูกเก็บใน `public/models/` (ไม่ต้องกังวลเรื่องลบ เพราะเป็นไฟล์ที่คัดลอกมา)

