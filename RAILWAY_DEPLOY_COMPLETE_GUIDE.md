# 🚀 คู่มือ Deploy บน Railway - Step by Step ละเอียดมาก

## 📋 สิ่งที่ต้องทำ (ทำตามลำดับ)

### ✅ ขั้นตอนที่ 1: ตรวจสอบว่า Variables ถูกตั้งค่าแล้ว

1. **ไปที่ Railway Dashboard:** [railway.app](https://railway.app)
2. **เลือก Project:** "ample-presence" (หรือชื่อ project ของคุณ)
3. **คลิกที่ Service:** "web"
4. **ไปที่ tab "Variables"** (ด้านบน)
5. **ตรวจสอบว่า `OPENROUTER_KEY` มีอยู่:**
   - ดูในรายการ variables
   - ต้องมี `OPENROUTER_KEY` = `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`

**ถ้ายังไม่มี:**
- คลิก **"+ New Variable"** (มุมขวาบน)
- **Key:** `OPENROUTER_KEY`
- **Value:** `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`
- คลิก **"Add"**

---

### ✅ ขั้นตอนที่ 2: Deploy Service

#### วิธีที่ 1: Deploy จาก Architecture View

1. **ไปที่ tab "Architecture"** (ด้านบน)
2. **คลิกที่ service card "web"** (การ์ดที่มี GitHub icon)
3. **ดูที่ panel ด้านซ้าย:**
   - ควรเห็น **"34 changes"** หรือ **"35 changes"** (ถ้าเพิ่ม variable ใหม่)
   - ควรมีปุ่ม **"Deploy"** หรือ **"Apply changes"**
4. **คลิกปุ่ม "Deploy"** หรือ **"Apply changes"**

#### วิธีที่ 2: Deploy จาก Activity Sidebar

1. **ดูที่ sidebar ด้านขวา "Activity"**
2. **คลิกที่ "34 changes in web"** หรือ **"35 changes in web"**
3. **ควรเห็นปุ่ม "Deploy"** หรือ **"Apply changes"**
4. **คลิกปุ่ม "Deploy"**

#### วิธีที่ 3: Deploy จาก Variables Tab

1. **ไปที่ tab "Variables"**
2. **หลังจากเพิ่ม/แก้ไข variables**
3. **ดูที่ panel ด้านซ้าย** (ถ้ามี)
4. **คลิก "Deploy"** หรือ **"Apply changes"**

#### วิธีที่ 4: Deploy จาก Settings

1. **ไปที่ tab "Settings"**
2. **ดูที่ "Deploy" section**
3. **คลิก "Deploy"** หรือ **"Redeploy"**

---

### ✅ ขั้นตอนที่ 3: รอให้ Deploy เสร็จ

1. **ดู progress:**
   - จะเห็น **"Deploying..."** หรือ **"Building..."**
   - ใช้เวลาประมาณ **2-3 นาที**

2. **ตรวจสอบ logs:**
   - ไปที่ tab **"Logs"** (ด้านบน)
   - ดู logs ว่า build และ deploy สำเร็จหรือไม่
   - ตรวจสอบว่าไม่มี error

3. **ตรวจสอบ status:**
   - ไปที่ tab **"Deployments"** (ด้านบน)
   - ดู deployment ล่าสุด
   - Status ควรเป็น **"Active"** หรือ **"Running"**

---

### ✅ ขั้นตอนที่ 4: ดู URL

1. **ไปที่ tab "Settings"** (ด้านบน)
2. **ดูที่ "Domains" section**
3. **Railway จะให้ URL เช่น:**
   - `https://ample-presence-production.up.railway.app`
   - หรือ `https://web-production-xxxx.up.railway.app`

4. **คัดลอก URL นี้ไว้**

---

### ✅ ขั้นตอนที่ 5: ทดสอบเว็บ

1. **เปิด URL ที่ได้จาก Railway**
2. **ทดสอบหน้าเว็บ:**
   - Betting Mode: `https://your-app.railway.app/rps_betting.html`
   - PvP Mode: `https://your-app.railway.app/rps_game.html`
   - VS Luna Mode: `https://your-app.railway.app/rps_vs_luna.html`

3. **ตรวจสอบว่าเว็บทำงาน:**
   - หน้าเว็บโหลดได้
   - ไม่มี error
   - API ทำงาน (ถ้ามี)

---

## 🔍 ถ้ายังหา Deploy Button ไม่เจอ

### ตรวจสอบว่า Service ถูกสร้างแล้วหรือยัง

1. **ดูที่ Architecture view**
2. **ตรวจสอบว่ามี service "web" หรือไม่**
3. **ถ้าไม่มี:**
   - คลิก **"+ Create"** (มุมขวาบน)
   - เลือก **"GitHub Repo"**
   - เลือก repository **"NOI56/Luna-moon"**
   - Railway จะ deploy อัตโนมัติ

### ตรวจสอบว่า Repository ถูกเชื่อมต่อแล้วหรือยัง

1. **ไปที่ tab "Settings"**
2. **ดูที่ "Source" section**
3. **ตรวจสอบว่า repository ถูกเชื่อมต่อแล้ว**
4. **ถ้ายังไม่เชื่อมต่อ:**
   - คลิก **"Connect GitHub"**
   - เลือก repository **"NOI56/Luna-moon"**

---

## ⚠️ ปัญหาที่พบบ่อย

### 1. หา Deploy Button ไม่เจอ

**แก้ไข:**
- ตรวจสอบว่า service ถูกสร้างแล้ว
- ตรวจสอบว่า repository ถูกเชื่อมต่อแล้ว
- ลอง refresh หน้าเว็บ (F5)
- ลอง logout และ login ใหม่

### 2. Deploy ไม่สำเร็จ

**แก้ไข:**
- ดู logs ใน tab "Logs"
- ตรวจสอบว่า variables ถูกตั้งค่าถูกต้อง
- ตรวจสอบว่า `railway.json` มีอยู่
- ตรวจสอบว่า `package.json` มี `start` script

### 3. Service ไม่ทำงาน

**แก้ไข:**
- ตรวจสอบ logs ว่าไม่มี error
- ตรวจสอบว่า PORT ถูกตั้งค่าถูกต้อง
- ตรวจสอบว่า environment variables ถูกตั้งค่า

---

## 📋 Checklist สุดท้าย

- [ ] Variables ถูกตั้งค่าแล้ว (`OPENROUTER_KEY`)
- [ ] Service ถูกสร้างแล้ว
- [ ] Repository ถูกเชื่อมต่อแล้ว
- [ ] คลิก "Deploy" แล้ว
- [ ] Deploy สำเร็จ (ดูใน Deployments tab)
- [ ] ได้ URL จาก Railway
- [ ] ทดสอบเว็บว่าทำงาน

---

## 💡 Tips

1. **ใช้ Keyboard Shortcut:**
   - กด `Shift + Enter` เพื่อ deploy (ถ้ามี)

2. **ดู Activity:**
   - ดูที่ sidebar "Activity" เพื่อดูการเปลี่ยนแปลง

3. **ตรวจสอบ Logs:**
   - ดู logs เพื่อ debug ปัญหา

4. **Restart Service:**
   - ถ้า service ไม่ทำงาน ลอง restart (Settings → Restart)

---

**ทำตามขั้นตอนด้านบน Deploy ได้แน่นอน!** 🚀

