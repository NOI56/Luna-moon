# 🚂 Railway Deploy - Step by Step Guide

## 📋 สิ่งที่ต้องเตรียมก่อน

1. ✅ **Code อยู่ใน GitHub แล้ว** (ถ้ายังไม่มี ดูขั้นตอนด้านล่าง)
2. ✅ **มีบัญชี Railway** (เข้าได้แล้ว)
3. ✅ **มีบัญชี GitHub**

---

## 🔄 ขั้นตอนที่ 1: Push Code ไป GitHub (ถ้ายังไม่ได้ทำ)

### ถ้ายังไม่มี GitHub Repository:

1. **สร้าง Repository บน GitHub:**
   - ไปที่ [github.com](https://github.com)
   - คลิก "New repository"
   - ตั้งชื่อ เช่น `luna-rps-game`
   - เลือก "Public" หรือ "Private"
   - คลิก "Create repository"

2. **Push Code จากเครื่อง:**
   ```bash
   # เปิด PowerShell หรือ Terminal ในโฟลเดอร์โปรเจกต์
   cd D:\LunaAI_v10_Project
   
   # ตรวจสอบว่าเป็น git repo หรือยัง
   git init
   
   # เพิ่มไฟล์ทั้งหมด
   git add .
   
   # Commit
   git commit -m "Initial commit - Ready for Railway deploy"
   
   # เชื่อมต่อกับ GitHub (แทน YOUR_GITHUB_REPO_URL ด้วย URL จริง)
   git remote add origin https://github.com/YOUR_USERNAME/luna-rps-game.git
   
   # Push ไป GitHub
   git push -u origin main
   ```

### ถ้ามี GitHub Repository แล้ว:
```bash
# เพิ่มไฟล์ใหม่ (ถ้ามี)
git add .

# Commit
git commit -m "Ready for Railway deploy"

# Push
git push origin main
```

---

## 🚀 ขั้นตอนที่ 2: Deploy บน Railway

### 1. คลิก "Deploy from GitHub repo"
- ในหน้า Railway dashboard
- คลิกไอคอน **GitHub** (ตัวแรกในแถว 4 ตัว)
- หรือคลิก "Deploy a GitHub Repository"

### 2. Login GitHub (ถ้ายังไม่ได้ login)
- Railway จะเปิดหน้าต่างใหม่
- คลิก "Authorize Railway"
- อนุญาตให้ Railway เข้าถึง repositories

### 3. เลือก Repository
- Railway จะแสดงรายการ repositories ของคุณ
- **เลือก repository ที่มี code ของ Luna RPS Game**
- คลิก repository ที่ต้องการ

### 4. Railway จะ Deploy อัตโนมัติ
- Railway จะ detect `railway.json` และ deploy ให้
- รอประมาณ 2-3 นาที
- ดู progress ใน dashboard

### 5. ตั้งค่า Environment Variables (ถ้าจำเป็น)
- ไปที่ tab "Variables"
- เพิ่ม environment variables (ถ้าต้องการ):
  - `NODE_ENV` = `production`
  - `PORT` = Railway จะกำหนดให้อัตโนมัติ (ไม่ต้องตั้ง)

### 6. ดู URL
- ไปที่ tab "Settings"
- ดู "Domains" section
- Railway จะให้ URL เช่น:
  - `https://luna-rps-game-production.up.railway.app`
- หรือคลิก "Generate Domain" เพื่อสร้าง custom domain

---

## ✅ ตรวจสอบว่า Deploy สำเร็จ

### 1. ดู Logs
- ไปที่ tab "Deployments"
- คลิก deployment ล่าสุด
- ดู logs ว่ามี errors หรือไม่
- ควรเห็น: `Luna v10 server listening on http://0.0.0.0:PORT`

### 2. ทดสอบเว็บ
- เปิด URL ที่ Railway ให้
- ทดสอบ:
  - `https://your-app.railway.app/rps_betting.html`
  - `https://your-app.railway.app/rps_game.html`
  - `https://your-app.railway.app/rps_vs_luna.html`

### 3. ตรวจสอบ WebSocket
- เปิด Browser Console (F12)
- เชื่อมต่อ Wallet
- ตรวจสอบว่า WebSocket เชื่อมต่อได้

---

## 🎯 สิ่งที่ต้องทำหลังจาก Deploy

### 1. แชร์ URL ให้คนอื่น
```
Betting Mode: https://your-app.railway.app/rps_betting.html
PvP Mode: https://your-app.railway.app/rps_game.html
VS Luna Mode: https://your-app.railway.app/rps_vs_luna.html
```

### 2. ตั้งค่า Custom Domain (ถ้าต้องการ)
- ไปที่ tab "Settings"
- คลิก "Generate Domain"
- หรือเพิ่ม custom domain ของคุณเอง

### 3. ตรวจสอบ Usage
- ดู tab "Metrics" เพื่อดู usage
- ตรวจสอบว่าไม่เกิน free tier limit

---

## 🔧 แก้ไขปัญหา

### ปัญหา 1: Deploy ไม่สำเร็จ
**แก้ไข:**
- ดู logs ใน tab "Deployments"
- ตรวจสอบ errors
- ตรวจสอบว่า `railway.json` มีอยู่
- ตรวจสอบว่า `package.json` มี `start` script

### ปัญหา 2: เว็บไม่ทำงาน
**แก้ไข:**
- ตรวจสอบ logs
- ตรวจสอบว่า PORT ถูกต้อง
- ตรวจสอบ environment variables

### ปัญหา 3: WebSocket ไม่ทำงาน
**แก้ไข:**
- Railway รองรับ WebSocket อัตโนมัติ
- ตรวจสอบว่าใช้ `wss://` (ไม่ใช่ `ws://`)
- ตรวจสอบ logs

---

## 📝 Checklist

- [ ] Code อยู่ใน GitHub แล้ว
- [ ] คลิก "Deploy from GitHub repo"
- [ ] Login GitHub และ authorize Railway
- [ ] เลือก repository ที่ถูกต้อง
- [ ] Railway deploy สำเร็จ (ดู logs)
- [ ] ได้ URL จาก Railway
- [ ] ทดสอบเว็บว่าทำงานได้
- [ ] แชร์ URL ให้คนอื่น

---

## 🎉 เสร็จแล้ว!

หลังจาก deploy สำเร็จ:
- ✅ เว็บจะทำงานออนไลน์ตลอดเวลา
- ✅ ไม่ต้องรัน `node index.js` เอง
- ✅ แก้ไขโค้ด → Push GitHub → Deploy อัตโนมัติ
- ✅ แชร์ URL ให้คนอื่นได้เลย!

---

## 💡 Tips

1. **ใช้ Branch สำหรับทดสอบ:**
   - สร้าง branch `staging` สำหรับทดสอบ
   - Deploy branch `main` สำหรับ production

2. **ดู Logs เป็นประจำ:**
   - ตรวจสอบ logs เพื่อดู errors
   - ดู metrics เพื่อดู usage

3. **Backup Database:**
   - ถ้าใช้ SQLite อาจหายเมื่อ restart
   - ควรใช้ Postgres addon ใน Railway

---

**พร้อมแล้ว! เริ่ม deploy ได้เลย!** 🚀

